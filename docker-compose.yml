services:
  frontend:
    build:
      context: .
      args:
        BASE_URL: /
    ports:
      - "8080:80"
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "8000:8000"
