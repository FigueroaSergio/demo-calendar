services:
  frontend:
    build:
      context: .
      args:
        BASE_URL: /
        API_URL: ${VITE_API_URL:-/solve}
    ports:
      - "8080:80"
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "8000:8000"
